// Public playbooks are intentionally empty until each displayed statement has
// a source-registry identifier and canonical public URL that supports it.
// Product-authored operating advice is not presented as source-truth content.
export const patternsData = [];
