# Control Atlas Backlog

- **Owner:** Product owner
- **Status:** Canonical open work only
- **Last reviewed:** 2026-08-13
- **Supersession:** Add, close, or reprioritize items here; do not create another backlog or preserve completed rows.

| ID | Open outcome | Owner | Trigger |
| --- | --- | --- | --- |
| EXT-A11Y-001 | Hands-on NVDA plus VoiceOver or TalkBack verification | Human QA | Before any claim of assistive-technology conformance |
| EXT-DEVICE-001 | Physical iOS and Android phone verification | Human QA | Before any claim of physical-device coverage |
| EXT-SEC-001 | Independent penetration test | Product owner | Before a formal external security-assurance claim |

Routine dependency, source-freshness, CI, Pages smoke, and comparative performance checks are operations, not backlog items.

## Detail and polish audit

Evidence labels below are deliberate: `live` means the canonical Pages site was inspected, `phone photo` means the supplied Android Chrome capture, and `static` means the current component or stylesheet contract was inspected. Re-test every responsive item at 320, 375, 390, 768, 1024, and 1440 pixels before closure.

| ID | Priority | Open outcome | Evidence | Acceptance criteria |
| --- | --- | --- | --- | --- |
| POLISH-007 | P1 | Close the legacy public-URL trap that led users to the obsolete `ashbryant.github.io/control-atlas/` release. | Both supplied phone photos show the legacy host; a current direct check returns GitHub Pages 404 while the canonical product is on `backslashbryant.github.io/control-atlas/`. The authenticated `BackslashBryant` account has no `ashbryant/control-atlas` repository to configure. | Keep the automated owned-link check that forbids the exact obsolete URL. If the legacy account or Pages property becomes available, publish a minimal canonical redirect; otherwise retain this external ownership limitation explicitly rather than representing the 404 as fixed. |
| POLISH-015 | P1 | Keep the compact record action menu inside the viewport when it is open. | `live` `#/record/nist-800-53/AC-2`: opening **More actions** at 375 px creates 42 px of document overflow and places the 166 px action links at `x=237.7..403.7`; at 390 px it creates 27 px of overflow. The same menu fits at 320 px. `static`: the `max-width: 900px` rule anchors `.record-actions-popover` to the action trigger's left edge even when that trigger remains on the right side of the wrapping action row. | At 320, 375, 390, and 768 px, opening **More actions** keeps every action fully visible without document or container overflow; the popover stays visually attached to its trigger, Escape closes it and restores focus, and a rendered regression test measures both viewport edges after expansion. |
| POLISH-016 | P1 | Eliminate the 1024–1199 px global-navigation dead zone. | `live` at 1024, 1030, 1100, 1119, 1120, and 1180 px: neither primary navigation nor **Open navigation menu** nor **Open more pages** is visible; 1023 px has the compact menu and 1200 px restores six primary links plus **Open more pages**. `static`: `TopNav` switches to compact navigation through 1199 px, while the CSS only reveals `.mobile-nav-toggle` through 1023 px. | At every width from 1024 through 1199 px, one visible, keyboard-operable navigation owner exposes Atlas, Library, Compare, Resources, Sources, About, and Guides. Automated boundary checks cover 1023, 1024, 1199, and 1200 px, verify exactly one navigation mode, and exercise destination navigation plus Back/Forward restoration. |
| POLISH-017 | P2 | Give Home the same global trust and release footer facts promised by the shared shell. | `live` `#/` at 390 and 1440 px renders only a 41 px `.home-footer` containing “Free and open source. Not a government system.” Every other audited route renders the shared footer with product-release date, source-data date, source attribution, contribution links, and problem reporting. `static`: Home owns a separate footer in `src/index.html`, while the page contract says Home may omit provenance copy because it is owned by About or the footer. | Home exposes the approved disclaimer, product-release date, source-data date, source attribution, and problem-reporting route without duplicating a second footer or materially increasing first-task distance. The shared-shell regression covers Home and all canonical route families at compact and desktop widths. |
| POLISH-018 | P2 | Normalize resource metadata labels instead of exposing lowercase enums and title-cased acronyms. | `live` Resources list rows expose maintenance values such as `active`, while resource detail renders governed values with naive title casing, including `General It` and verification method `Public Url`; the **Access** section also repeats **Access** as its first fact label. `static`: `CommonsPage` uses `maintenanceStatus.replaceAll("_", " ")` and `CommonsDetailPage.display()` uppercases word initials without preserving `IT` or `URL`. | Resources list, comparison, contextual cards, and detail use one governed label formatter for status and verification enums; known acronyms remain `IT`, `URL`, `API`, and similar approved forms; statuses use consistent sentence/title case; and section/fact labels do not read as accidental duplicates. Contract tests enumerate every resource enum value and representative acronym-bearing labels. |
