# Control Atlas V1 Goal-mode objective

Use Codex with `gpt-5.6-sol` at `xhigh`.

Paste this as the `/goal` command. It is intentionally under Goal mode's 4,000-character limit; the detailed instructions remain in repository files.

```text
/goal Deliver the true Control Atlas V1 locally from the current HEAD of agent/muse/control-atlas-full-surface-audit. Work on agent/forge/control-atlas-holistic-correction. Treat docs/planning/control-atlas-holistic-correction-execution-prompt-2026-07-28.md as the execution instructions and docs/planning/control-atlas-v1-launch-contract-2026-07-28.md as the definition of done.

Implement every correction milestone continuously. For each defect class, prove the failure, identify the systemic root cause, add the cheapest faithful failing invariant/test, fix the shared owner, migrate every consumer, delete superseded implementations, verify affected states and responsive surfaces, then move to the next item. Misattribution must fail closed across the full corpus. The product must never determine applicability, baseline, compliance, inheritance, authorization, or ATO outcomes.

Do not stop at green tests or closed backlog rows. Finish only when every required launch-contract gate that can be completed locally passes, no known Critical/High defect remains, the release evidence packet is current, milestone commits exist, and the worktree is clean. Keep human practitioner, screen-reader, physical-device, deployment, and owner GO evidence explicitly pending when external action is required.

You may run the authorized local server and non-destructive validation described in the execution prompt. Do not push, merge, deploy, release, modify production, rewrite history, edit secrets, or change files outside the repository. Stop only for an unsafe action needing new authority, an unreconcilable overlapping user edit, a governing-doc conflict requiring a product decision, or a repeated external blocker after safe alternatives are exhausted.
```
