# Codex prompt — holistic Control Atlas correction

Date: 2026-07-28

## Exact setting

- Model: `gpt-5.6-sol`
- Reasoning: `xhigh`
- Use `Max` only if xhigh becomes blocked on the systemic source-identity, graph, or state-model work.
- Do not use Ultra for the initial run.

`xhigh` preserves strong root-cause reasoning while leaving more of the weekly allowance for implementation, browser proof, and recovery from a failed gate.

## Paste this into Codex

---

Implement the complete Control Atlas correction in:

`D:\DevOps\1. Projects\GovFrame`

This is an authorized change/build task, not another audit. Execute the whole correction program in one continuous session. Complete each milestone and its regression gate, then move directly to the next without asking for approval.

## Governing package

Read completely before editing:

1. `AGENTS.md`
2. `C:\Users\OrEo2\.engineering\core-engineering-doctrine.md`
3. `docs/audits/control-atlas-full-surface-audit-2026-07-28.md`
4. `docs/design/control-atlas-target-experience-2026-07-28.md`
5. `docs/planning/control-atlas-ux-correction-backlog-2026-07-28.md`
6. `artifacts/audits/control-atlas-2026-07-28/surface-matrix.csv`
7. `artifacts/audits/control-atlas-2026-07-28/copy-register.csv`
8. `artifacts/audits/control-atlas-2026-07-28/oss-evaluation.md`
9. `docs/planning/control-atlas-v1-launch-contract-2026-07-28.md`
10. `docs/research/control-atlas-v1-practitioner-validation-protocol-2026-07-28.md`

Treat those files as the execution contract. Read other project documents only when the package or the current code points to them. Do not repeat their contents in chat or create another plan.

The newer correction rules supersede older PRD language about synthetic summaries or framework/baseline recommendations.

## Branch and authority

- Start from audit commit `2e8bc89c86a7124a1c222896b7585f311abfef6e`.
- Work on `agent/forge/control-atlas-holistic-correction`, never `main`.
- Preserve unrelated user changes.
- You may edit all in-repository code, data builders, generated-data contracts, copy, styles, tests, and current planning/state documentation needed for the correction.
- You may run non-destructive generators, builds, tests, browser checks, and accessibility checks.
- For browser work: run `npm run ports:status`; if port 3000 is free, you are authorized to start exactly `npm run dev:guarded` on port 3000 and stop only that server with the approved repository command.
- Commit coherent verified milestones locally.
- Do not push, merge, deploy, tag, release, modify production, rewrite history, edit secrets, or change files outside the repository.

## Execution method

Create one compact traceability ledger mapping each Critical/High finding to:

`root cause → shared owner → failing test/invariant → changed consumers → verification → status`

Use the milestone order and acceptance criteria already defined in the correction backlog:

1. Stop untruthful output.
2. Restore canonical destination and URL-state contracts.
3. Rebuild first-screen architecture.
4. Finish Explore, Catalog, Compare, and Learn.
5. Consolidate copy, search, style, and presentation ownership.
6. Run final product proof.

For each related defect class:

1. Reproduce or verify the reported failure.
2. Use `rg` to find the generating data/model/component, direct consumers, duplicate implementations, and current tests.
3. Decide whether the named example is systemic.
4. Add the cheapest faithful failing invariant or test.
5. Fix the most central owner of the truth.
6. Migrate every affected consumer and delete the superseded route, index, component, copy source, selector, style, or test.
7. Run focused tests.
8. Recheck affected routes plus loading, zero, error, invalid, recovery, mobile, and desktop states.
9. Update the ledger and continue.

Do not fix only the quoted example. Do not leave known sibling instances. Do not add a parallel taxonomy, Search, graph model, route identity, copy source, state system, or design system.

## Non-negotiable boundaries

- **Misattribution can never happen.** Separate ingestion provenance from exact publication identity for every catalog. Validate the full catalog-to-publication mapping. If exact identity is unavailable, fail closed and never guess.
- Never select or infer applicability, baseline, compliance, inheritance, authorization, or ATO outcomes. Remove the silent Moderate default everywhere. Preview and Download must share one validated input state.
- Visible label, URL, title, selected navigation, analytics, context, and recovery destination must come from one canonical destination identity.
- Meaningful search, filter, scope, task, document, comparison, focus, and lens state belongs in validated URLs.
- Trees contain only publisher-declared hierarchy. Baselines, mappings, applicability, evidence, implementation aids, processes, and Resources are never parents. RMF is optional.
- Search eligibility precedes ranking. Exact unique identifiers may navigate directly; ambiguous text goes to Search; zero remains zero.
- Keep and consolidate MiniSearch and React Flow/ELK unless a measured blocker proves replacement necessary. Delete duplicate ownership.
- Apply the copy register by defect class. Official text is not rewritten. Preserve the owner-protected rotating Ctrl+Alt brand flourish while removing other patronizing labels, platitudes, slogans, metaphors, generic marketing, vague actions, unsupported recommendations, and disclaimer repetition.
- Implement the target layouts, density, and responsive sequence—not only replacement words. Mobile and 200% zoom retain every control, warning, source field, and piece of information.

## Efficiency rules

- Do not reread or restate the governing package after the initial pass; use the ledger as working memory.
- Combine findings that share one root cause and fix them once.
- Use fast data/schema/unit/reducer/graph/copy contracts as the inner loop.
- Run an expensive build/browser group only after its focused tests are green.
- If an expensive loop fails twice without narrowing the fault, add a focused diagnostic before rerunning it.
- Run browser verification once per completed milestone and one final full pass.
- Use context compaction at milestone boundaries and continue from the ledger.
- Do not pause for routine progress updates or approval between milestones.

## Completion gate

Do not call the pass complete until:

- every correction-backlog milestone gate passes;
- every locally achievable V1 launch-contract gate passes and the remaining external gates are recorded without false completion;
- all Critical findings are fixed systemically and no known Critical/High residual remains;
- the catalog-wide publication identity audit has zero mismatches;
- all twelve practitioner workflows pass;
- all 56 surface-matrix rows and applicable copy-register rules are reconciled;
- Path, Map, and List use the same declared Explore scope;
- Home, Catalog, Record, Compare, Build/Resources, Learn, and Sources match the target hierarchy at 375, 768, 1440, and actual 200% zoom;
- keyboard, reduced-motion, automated accessibility, loading, zero, error, invalid, and recovery evidence is recorded honestly;
- superseded code/tests/data/styles are removed;
- focused gates and `npm run precommit` pass;
- milestone commits exist and the worktree is clean.

Human screen-reader or physical-device evidence may remain explicitly unverified if unavailable. Nothing else is complete merely because a test is green.

Stop only for an unsafe action needing new authority, an unreconcilable overlapping user edit, a governing-doc conflict requiring a product decision, or a repeated external blocker after safe alternatives are exhausted.

## Final response

Lead with the product outcome. Report completed milestones, root causes corrected, obsolete systems removed, acceptance evidence, exact tests/results, responsive/accessibility evidence, changed files, migrations/rollback, residual external evidence, branch, and commits.

State explicitly that no push, merge, deployment, release, or production mutation occurred.

---
