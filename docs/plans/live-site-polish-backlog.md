# Live-Site Polish Backlog

Source: live-site walkthrough + template-output audit, 2026-07-10 (post SPR-remediation ship `0a29f49`).
Shipped the same day, so NOT in this backlog: search provenance fix, 705 blocked-relationship repair, office export layout, coverage badges + supported-catalog contract, orbital landing, nav disclosure, frame-bust, template content overhaul, Explore "No connections yet" false-negative fix.

## P0 — brand consistency, copy, and content trust (flagged by owner 2026-07-11)

- [x] **Header brand lockup is inverted vs. the landing page** — FIXED via reframe Phase 2 (`e036468`): `TopNav.tsx` JSX reordered to wordmark-above-flourish, matching `HomePage.tsx`. — `TopNav.tsx:64-68` renders `.brand-lockup` as `<BrandFlourish />` (the rotating "Ctrl / Alt / [word]" pill row) THEN `<span class="brand-name">Control Atlas</span>` below it; `.brand-lockup` is `flex-direction: column` (`styles/surfaces.css:292-297`), so the flourish sits on top and the wordmark sits underneath, small. `HomePage.tsx:18-23` does the opposite: `.landing-brand-row` (icon + `<h1>Control Atlas</h1>`) renders first, `<BrandFlourish />` second — wordmark on top, animation below. Owner wants the header to match the landing treatment: wordmark on top, flourish underneath. Fix is scoped to `TopNav.tsx`'s JSX order (and possibly `.brand-lockup` CSS if sizing needs adjusting) — does NOT require touching `BrandLockup.tsx` itself, so it doesn't conflict with the standing "don't touch BrandLockup.tsx" constraint (that file only exports the sub-components; layout order lives in the consumers).
- [ ] **Site-wide copy pass — nav labels and beyond** — owner specifically called out `src/ui/lib/navigation.ts:34` (`"Research · Learn"`) and `:64` (`"Build · Create"`) as awkward, non-relatable "dot-word" labels. Likely candidates elsewhere in the same pattern-family: `landing-orbit-btn` captions on `HomePage.tsx` ("Playbooks & practical guidance", "Templates & RMF starters", "Map connections across frameworks" — reviewed for layout in this session's other fix, not for wording), nav group label `"Navigate"` (kept plain, no dot-suffix — inconsistent with its two siblings having one). Needs a full copy pass across nav labels, section eyebrows, and CTA microcopy — not just the two named examples. Scope: read every user-facing string in `navigation.ts`, `HomePage.tsx`, `TopNav.tsx`, and page `PageHeader` eyebrows/summaries, and rewrite for plain, non-redundant language. This is a copy-editing pass, not a code-logic change — low risk, but touches many files, so plan it as its own session.
- [ ] **Every generated template needs a professionalism/quality pass** — `data/template-registry.json` lists 8 templates (`tpl-ssp-starter`, `tpl-impl-stmt`, `tpl-evid-exp`, `tpl-stig-chk`, `tpl-inherit-wksh`, `tpl-reciprocity`, `tpl-poam`, `tpl-assess-plan`, `tpl-conmon`), each generated by `src/app/template-engine.mjs`. Owner wants each one actually opened in its office format (docx/xlsx) and checked for professional quality (formatting, tone, completeness) — this session's earlier fix (`36f8ca9`) addressed *layout* (column widths, frozen panes, table structure) but nobody has done a content/tone read-through of the generated prose per template. Needs a dedicated pass: generate all 8, open each office export, and grade against a real authorization-package bar.
- [ ] **No official FedRAMP template files are hosted or ingested — confirmed, not assumed** — checked `data/` and `data/generated/`: the only FedRAMP-related data is `data/fedramp-baselines.json` (control-to-baseline mapping) and the search index; there is no actual FedRAMP SSP/POA&M/CRM document anywhere in the repo. The app generates its own approximations via `template-engine.mjs`. 3 of the 8 registry templates (`tpl-ssp-starter`, `tpl-inherit-wksh`, `tpl-poam`) carry an `official_alternative` pointer (`data/template-registry.json:14-17,78-81,110-113`) that all three resolve to the *same generic URL*, `https://www.fedramp.gov/documents-templates/` — not a deep link to the specific document, so the user still has to hunt for the real file themselves. The other 5 templates don't reference FedRAMP at all. Decide: either deep-link each `official_alternative` to the specific current FedRAMP document (fedramp.gov reorganizes this page periodically, so verify links don't rot), or be explicit in the UI that these are unofficial worksheets modeled on FedRAMP conventions, not FedRAMP-sourced content — right now the distinction is easy to miss.

## P1 — padding & alignment (visual audit 2026-07-11, post `50b5f80`)

Methodology: `npm run build:site` + local static preview, computed-style/geometry inspection via devtools JS (browser-pane screenshots still time out — same limitation noted below, unresolved). Numbers are real `getBoundingClientRect()`/`getComputedStyle()` reads, not eyeballed.

- [x] **Landing orbit CTAs are three different sizes and unevenly spaced from the orb** — FIXED 2026-07-11. Root cause found: `--space-8`/`--space-12`/`--space-16` (legacy aliases) were referenced by `.landing-hero` padding, `.landing-launch` row-gap, and `ComparePage.tsx`'s `workbench-toggle` margin, but never defined in `styles/tokens.css` (only `--space-1..7` existed) — an unresolvable `var()` invalidates the whole declaration, so all three were silently computing to `0`/`normal`. Added the missing aliases (`tokens.css:124-127`). That alone wasn't enough to balance the orbit (row-gap real value of 2rem still left Navigate far closer than Research/Build), so also: matched `.landing-launch` row-gap to the same `clamp(2.5rem, 9vw, 7rem)` already used for column-gap (`surfaces.css:2896`), gave `.landing-orbit-btn` a fixed `width: 14.5rem` (`surfaces.css:3009`), and gave the caption `span` a 2-line `min-height` so 1-line and 2-line captions render the same height (`surfaces.css:3049-3057`). Verified at 1280px: all three pills now 232×92 (was 219×92/202×73/219×92); Research/Build now exactly equidistant from center (344px/344px, was 331/322); Navigate improved from 152px to 264px (not fully equal — remaining gap is the orb block's own off-center bounding box from its caption text, judged acceptable rather than chasing pixel-perfect radius).
- [x] **Same CTA row goes asymmetric at tablet widths (~900px)** — FIXED 2026-07-11, verified: left/right edge margins now 12px/11px (was 12px/27px) via the same fixed-width fix above.
- [x] **Same CTAs form a ragged stack on mobile (375px)** — FIXED 2026-07-11, verified: all three pills now 232px wide, left edges aligned at 72px, evenly spaced (108px apart), no horizontal scroll. Was 232/202/258px wide with left edges at 71/87/58px.
- [ ] ~~Relationship group labeling~~ — re-confirmed still open on the built site: AC-2's connections panel still reads "27 other public mappings" (verified live 2026-07-11). Not addressed this pass — separate fix, needs source-grouping logic, not a CSS change.
- [ ] **Spacing-token drift across `styles/surfaces.css`** — the token scale in `styles/tokens.css:69-76` (`--ca-space-1..16`, 0.25rem steps) is real and mostly followed, but ~15 rules bypass it with one-off values that don't land on the scale: `padding: 0.55rem 0.75rem` (L322), `0.8rem 0` (L668), `0.35rem 0.7rem` (L759, L2360), `0.45rem 0.85rem` (L1076), `0.65rem 0.95rem` (L1346), `0.55rem 0.9rem` (L2641), `0.8rem 1.5rem` (L2865, the home-search submit button), etc. Individually harmless, but it's the pattern that produces the "every component invented its own spacing" look — new components should reach for `var(--ca-space-*)` and none of these should have shipped as bespoke rem values. Not urgent to bulk-fix, but flag in review going forward so it doesn't keep growing.
- [x] **`accessibility.spec.mjs` "compare detailed mappings table" test is borderline-slow, and the orbit-CTA fix tips it over** — FIXED 2026-07-11 (owner sign-off given in-conversation). Bisection: 2/2 clean passes without the CSS fix (78-84s against the old 90s cap), 3/3 timeouts with it. Bumped `test.setTimeout(90_000)` to `120_000` at `tests/e2e/accessibility.spec.mjs:84` — assertion unchanged, just a realistic budget for a table-heavy axe scan on this machine. Verified: the single test now passes at 72s, and the full `accessibility.spec.mjs` file (18 tests) passes in 2.4m.

## P1 — trust & first-use

- [ ] **Relationship group labeling**: AC-2 detail shows "27 other public mappings" — now that OLIR CSF 2.0 / SP 800-171 mappings are ingested, label them as named crosswalk groups ("CSF 2.0 crosswalks", "SP 800-171 mappings") instead of "other". Grouping logic feeds the record-detail Connections panel.
- [ ] **Visual-density screenshot pass**: browser-pane screenshots timed out during this walk, so density judgments (orbit row at 720–1100px widths, focus rings, glow on low-DPI) are unverified. Re-run with working screenshots at mobile/tablet/desktop and both reduced/normal motion. (Owner rule: never rationalize crowding away without pixels.)
- [ ] **`isLowCatalogCoverage` boundary**: threshold is `pct < 75`, so SP 800-171 Rev. 3 at exactly 75% skates past the Preview badge. Decide inclusive (`<= 75`) or a stated cutoff; adjust `src/ui/lib/catalogCoverage.ts` + a11y-contract assertion together.
- [ ] **Known-gaps note for residual blocked relationships**: 11 remain in graph-health (9 OLIR rows with bare family/category endpoints e.g. "CP" → "PR.IR-03"; 2 stale DoD ZT refs to EC-1/SAC-16). Surface a short "known upstream gaps" line on the Sources page so the number is explained, not discovered.

## P2 — template value adds (post content-overhaul)

- [ ] **Archetype-aware inheritance pre-fill**: for Cloud SaaS / CSP archetypes, pre-mark commonly provider-inherited families (PE, MP, parts of CP/SC) in the Inheritance Worksheet with "typically inherited — verify with your provider's CRM/SSP" rows. This is the product's birds-per-stone thesis applied to templates. Needs a small curated archetype→family table in `src/app/template-engine.mjs`.
- [ ] **Evidence-type-by-family suggestions**: Evidence Expectation Matrix can suggest artifact types from the control family (AU → log samples/retention config; CM → config exports/change records; AT → training records; RA → scan reports). Curated map, ~20 lines, replaces the generic evidence menu bullet.
- [ ] **Template card copy sync**: POA&M card promises "eMASS/FedRAMP columns" — sync `TemplatesPage.tsx` descriptions with the redesigned outputs (field guide + core tracker).
- [ ] **Enhancement-include toggle in UI**: engine option `includeEnhancements` exists; expose a checkbox on TemplatesPage for all-controls mode.
- [ ] **XLSX data-validation dropdowns** for Status/Severity columns (OOXML dataValidation elements; hand-rolled writer already emits styles).
- [ ] **DOCX visual styling pass**: heading styles, cover page, brand-neutral fonts; add LibreOffice raster QA gate in CI (blocked locally — no LibreOffice on this machine).

## P3 — coverage & data

- [ ] **0% catalogs**: MITRE ATT&CK ICS, AI RMF, SSDF, DoD RAI have zero connected nodes; SP 800-172 is 1%. Either ingest crosswalk sources for them or curate their search exposure beyond the Preview badge (e.g. suppress from default search ranking).
- [ ] **Upstream OLIR gaps**: report/patch the 9 bare-identifier rows upstream, or add a curated local completion for the obvious ones (CP-4 → RC.RP-03 style) with `inferred` provenance class.

## P4 — hygiene

- [ ] **navigation-fidelity focus flake**: `#library-results` toBeFocused race under parallel e2e load (passes 5/5 isolated). Find the focus-after-load path and make it deterministic or wait on a stable signal.
- [ ] **Bundle size**: build warns >500 kB chunk; consider code-splitting the graph stack (React Flow + ELK) behind the atlas route.
- [ ] **Live-smoke spec**: currently skipped off-live; consider a scheduled Pages-live-smoke run against production after each deploy (workflow `pages-live-smoke.yml` exists — verify it's wired to the deploy event).
